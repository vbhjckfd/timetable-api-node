TRIP_UDPDATES_URL=https://track.ua-gis.com/gtfs/lviv/trip_updates

SENTRY_DSN=

# Optional. Base URL of this API for MCP initialize metadata (icon URL). Default: https://api.lad.lviv.ua
# MCP_PUBLIC_BASE_URL=https://api.lad.lviv.ua

# Pulse WebSocket worker — signal emitted on each /stops/:code/timetable request.
# PULSE_WORKER_URL=https://timetable-pulse.<account>.workers.dev
# PULSE_SIGNAL_SECRET=  # must match the value set via: wrangler secret put PULSE_SIGNAL_SECRET
